import { Map } from './Map';

const parameters = {
  title: 'Component/Map',
  component: Map,
};

const Template = (args) => <Map />;

export const Primary = Template.bind({});
export default parameters;
